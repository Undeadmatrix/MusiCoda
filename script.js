$(document).foundation();

$(document).ready(function() {
    //create functions to hold query params
    //pass those function returns to the ajax call



    $.ajax({
        url: queryURL,
        method: "GET"
      }).then(//function to display on page);


      
})